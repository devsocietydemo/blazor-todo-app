@page "/todo"

<h3>Todo (@todos.Count(todo => !todo.IsDone)) </h3>
<ul>
    @foreach (var item in todos)
    {
        <li>
            <input type="checkbox" @bind="item.IsDone" />
            @item.Title
        </li>

    }
</ul>
<input @bind="newTodo" placeholder="What you're about to do" />
<button class="btn" @onclick="AddTodo" >Add todo</button>

@code {
    private List<TodoItem> todos = new List<TodoItem>() 
        {
            new TodoItem { Title= "Buy milk"},
            new TodoItem { Title= "Change tires"}
        };
    private string newTodo;
    
    public void AddTodo()
    {
        if(!string.IsNullOrEmpty(newTodo))
        {
            todos.Add(new TodoItem { Title = newTodo, IsDone= false});
            newTodo = string.Empty;
        }
    }

}
